# Appendix F. The axis ledger

The specification in [appendix A](A-audit-prompt.md) defines 22 axes, and each
one carries a **Bar**: the condition under which that axis is closed. The
specification also names three conditions for stopping, and the first is that
every axis is *closed, or open with a number and a named reason*.

Nothing recorded that. The axes live in one file, the findings live in chapters
organised by subject, and no index connected them, so the first stop condition
could not be evaluated at all. Not because the work was missing: chapter 13
publishes upwards of forty numbers and several of them answer a Bar exactly.
What was missing is the row that says which.

This file is that index. It is kept honest by
[`axis-ledger.mjs`](D-scripts/axis-ledger.mjs), which fails if the specification
grows an axis this table does not have, or if this table names one the
specification does not.

## How to read a verdict

- **closed** — the Bar is met and a published number says so.
- **open, with a number** — the Bar is not met, and how far off is measured.
  This is a legitimate resting state under the stop condition. It is not a
  failure, it is the condition being satisfied honestly.
- **no verdict recorded** — nobody has compared this axis to its Bar. This is
  the state the stop condition forbids, and it is the most common state here.

A verdict is entered only where a number in this repository supports it. Where
a subject is discussed at length but never measured against the Bar, the
verdict stays unrecorded, because the Bar asks for a number and prose is not
one. That rule is what keeps this table from becoming a second opinion about
work rather than an index of it.

## The ledger

| Axis | Bar, in short | Verdict | Evidence |
|---|---|---|---|
| A1 | Rows in equals translated plus skipped plus errored, every skip carrying a reason; zero skips reasoned "same language as target" unless the detector was looked up; errored rows show the reader something; zero mispairings under recycling; a heterogeneous batch declares nothing; every dimension that changes the answer is in the cache key, proven by a collision test | open, with a number | Silent skips: 11 of 663 pairs, 1.7 percent, all at non-English targets. The accounting identity itself is never checked. Heterogeneous batches: **met**, the 2.10.0 changelog records a batch that borrowed the first message's fields and the fix that makes disagreeing hints declare nothing. Recycling: `buildSyntheticId` hashes `data-index` with the username and a text prefix, which is the mispairing guard. **The cache key holds 2 of at least 6 dimensions that change the answer.** It is `` `${targetLang}::${normalizeForKey(text)}` ``, and the provider, the source-language hint, DeepL's formality and DeepL's channel context all change the output and none is in it. The provider is **stored in the entry and unused in the key**, so switching provider returns the previous one's answer, correctly labelled with the previous one's name, and nothing invalidates the cache on a settings change: the only clear is a button. The collision test exists and is thorough about the two dimensions the key has ([4.79](E-method-log.md)) **[new] The clause about looked-up detectors is measured now** ([4.109](E-method-log.md)), and it fails. `detectLanguage` calls the same lookup `confidentLanguage` does, and when the Cyrillic guard **refuses** a Mongolian line, returning `undefined` on purpose because franc-min does not carry Mongolian at all, it reads that silence as absence of information and falls through to franc anyway. **4 of 25 refusals on an independent bench are overwritten**, three as `ru` and one as `bg`, and **3 of those 4 are then skipped as “already in your language”** at a Russian target, which is the one skip this bar says must never rest on a guess. The 21 refusals that survive give `lang_unknown`, 21 of 21, so the honest path works and only the overwritten ones lie. `undefined` is carrying both *I do not know* and *do not guess*, and only one caller can tell them apart. **[new] A skip reason that is false, measured** ([4.113](E-method-log.md)). The keyboard-smash filter drops a message through `isNoise` before any detection, telling the reader *it is only emoji, symbols or laughter*. Run over the product's own shipped interface text, **77 of 3438 words in nine locales are classified as smash**, including **`Traduire`**, this extension's own French label for Translate, and **`porque`** in Spanish and Portuguese. Six are flagged everywhere and are untranslated English; above that floor the count is French +9, Portuguese +7, Spanish +6, Turkish +1 and **zero for every non-Latin script**, which cannot reach the rule at all. **Two candidate fixes were planted and both failed in opposite directions**: adjacency takes false positives to 0 and loses every non-walking mash, a vowel floor keeps the mashes and loses the walks. The looseness is load-bearing. |
| A2 | Held-out bench, zero wrong for any unambiguous script | open, with a number | [13.2](../thesis/13-results.md): ar, ja, ko, ru all 25 of 25; **zh 24 of 25**, so the zero is missed by one on an unambiguous script. Latin half es 3/8, fr 4/5, pt 3/6, tr 1/4, which the Bar permits only with the trade written as a number, and it is: 8 silent losses of 483 pairs against 0 of 1125 **[new] Re-taken on a bench written here** ([4.109](E-method-log.md)), sharing no line with the corpus: Russian, Ukrainian, Persian and Arabic **10 of 10** each and Bulgarian **6 of 10**, against the corpus's 7 of 12; the Mongolian guard fires on **25 of 30** against its 17 of 20. **Three figures replicated independently**, which is the first time any published detection number here has been confirmed by an instrument the developing account did not build. **[new] The refusal is a class, not an instance** ([4.110](E-method-log.md)): an AST census of the detection path finds **11 returns of `undefined`, 5 of them refusals** guarded by a test on the text's own content, and the five do not want the same thing. The pure-Han branch asks to be overruled, *defer to franc so Chinese isn't mislabelled as Japanese*, and is, 0 of 10 surviving. Urdu and Mongolian ask to stand; Urdu stands **10 of 10** and Mongolian **6 of 10**. **The difference is not a mechanism**: Urdu stands only because franc answers `urd`, which `francToIso2` cannot map, while Mongolian falls because franc answers `rus`, which it maps cleanly. Franc is right about Urdu and wrong about Mongolian, and the one it is wrong about is the one that breaks. Adding **one line** to `FRANC_MAP` in another file takes Urdu from 10 of 10 to 1 of 10. **[new] All five refusals are measured now** ([4.111](E-method-log.md)). The two vote conflicts, a line whose words vote for different languages and one whose romanisation markers do, each survive **6 of 10**, so three of the four refusals that want to stand leak at **4 of 10 each**. The rate is not a property of any guard: it is how often franc lands inside the 42 languages on short chat text. The harm is sharper than Mongolian's because the leaked code is right about half a present language: `gracias merci` is declared **es** and a Spanish-target reader loses it as *already in your language*, French half and all. **The controls replicate two published figures**: on unanimous lines the product is **5 of 5** both times where franc alone is 2 of 5 and **0 of 5**, the second being the corpus's own *0 of 5 → 5 of 5*. `SHORT_WORD_LANG` has **no repeated keys** in 136 pairs, where a repeat would silently keep the last. **[new] The corpus's *zero false positives* for arabizi is true of its 29 traps and not of this platform** ([4.112](E-method-log.md)). The shipped rule is three characters, one of `3 5 7 9`, two letters; on sixteen lines of ordinary English gaming chat it declares **10 Arabic**, including `ak47`, `cloud9`, `ps5`, `gta5` and `mp5`. All ten reach the detector, checked in the pipeline's own order. The positive control was taken first and holds at **10 of 10** real arabizi, so this is not an argument for removing a feature that repaired a real harm. Consequence, driven: **10 of 10** dropped as *already in your language* at an Arabic target, and **10 of 10** sent to a provider for an English reader with *skip English* on. A planted rule forbidding a word-final digit takes the false positives to **1 of 16** with the control unchanged. |
| A3 | What is sent is exactly what the preview showed; handles, emote codes and URLs character-identical; engine calls per typed character under the ceiling in the budget file; the preview never occludes the composer | **satisfied as worded, and the failure its own axis names is reachable** | Handles and URLs are sound by construction: `maskProtected` replaces them with `⟦n⟧` sentinels before the request, so the engine never sees them, and `unmaskProtected` restores them from the token array at `compose.ts:357` before the text becomes `lastTranslation`. The insert path is sound too: `handleInsert` inserts `lastTranslation`, the same value the preview rendered, so what is sent is exactly what the preview showed. **The bar is met and the axis's own *breaks as* sentence, "sends the preview text rather than their own", is still reachable**: when the composer's limiter refuses, `compose.ts:323` keeps the last preview on purpose and `setComposeThrottle` only writes `dataset.throttled`, a styling hook, so the panel stays visible, `isComposePreviewVisible()` stays true, `Ctrl+Enter` still routes to `handleInsert`, and `handleInsert` guards on nothing but the two values existing. The bar protects preview-to-insert agreement; the risk is preview-to-composer disagreement, which no clause names ([4.59](E-method-log.md)). **The bar's *character-identical* clause is not provided for scheme-less URLs** ([4.97](E-method-log.md)): it rests entirely on `maskProtected`, which returns `tokens: []` for `kick.com/somechannel` and every shape without a scheme ([4.91](E-method-log.md)), so nothing is protected, nothing is restored, and whatever the engine returns is what the reader sends. The guarantee is delegated to the provider. Calls per typed character: **bound 1.000, worst constructed case 0.889** (24 of 27 on one long word), measured offline by [`compose-calls.mjs`](D-scripts/compose-calls.mjs) rather than by the live harness the row named ([4.90](E-method-log.md)). **A bare link costs 7 calls and four emotes cost 7**, both messages the guards skip once complete: `isLinkOrMentionOnly` recognises the URL only from `https://e` onward, so `ht` through `https://` go to a provider as ordinary text. The guards read complete tokens and the cost is paid per prefix. What binds depends on typing speed, and for most readers it is the limiter: `compose.ts:320` consults `RateLimiter(COMPOSE_MAX_PER_MIN = 30, 60_000)` before the request, and measured across a simulated minute it caps the path at 30 a minute from roughly 10 to 40 words a minute. Below that the gate chain binds, above it `COMPOSE_DEBOUNCE_MS = 320` collapses a message into its last prefix. An earlier version of this row credited the debounce alone, which [4.92](E-method-log.md) corrects |
| A4 | Every selector has at least one fallback or a named reason it cannot; every navigation shape ends with the observer attached and a row translated, proven by a count, with the route change's other effects asserted too | open, with a number | **4 of the 5 host-page selector groups carry a fallback chain**, and they are careful: `containers` has six candidates under an explicit *Legacy fallbacks* comment, `COMPOSE_SELECTORS` has seven ordered most-specific-first with the failure mode named (*the feature simply doesn't mount (graceful no-op)*), `extractUsername` tries four in order each commented with the Kick variant it covers and one dated against live chat in 2026-08, and `pickInjectionTarget` ends on the row itself so it cannot fail. **The fifth has neither**: `messageRows: ['div[data-index]']`, one selector, no alternative and no stated reason for having none, and it is the load-bearing one. If Kick stops emitting `data-index` the container is still found by six candidates, the observer still attaches, and no row is ever identified, which is the axis's *breaks as* sentence word for word ([4.60](E-method-log.md)). Third-party DOM is enumerated: the file's header names 7TV's token spans and observer class. The navigation half is open in the corpus's own queue under the name *`translate-navigation` asserts less than its name promises*, with `nav-monde.mjs` written to make the four assertions and deliberately not on master |
| A5 | No state lives only in worker memory; every write merges rather than overwrites; after eviction the first message translates within the ceiling in the budget file | open, with a number | Seven pieces of mutable module-level state in the worker, of which **six live only in memory**. `settings` is the exception and is reloaded with `loadSettings()` on start. Four of the six are round-robin indices and quota caches whose loss costs nothing. The seventh matters: `channelBuckets` holds a `TokenBucket` per channel, consulted before every request, and `bucketFor` recreates it **full** after a restart, so the per-channel budget (default 200 a minute) resets on every eviction. The keepalive that would prevent the eviction asks for `KEEPALIVE_INTERVAL_SEC = 25`, which is `periodInMinutes` 0.4167, **below the 0.5 minimum `chrome.alarms` documents** **[outside]**, so the interval it gets is not the interval it asks for, and no comment in the repository records the clamp. The bar's third clause names a ceiling in the budget file, and there is no budget file (4.54). The runtime witness the axis asks for, killing the worker between two messages, has never been run ([4.56](E-method-log.md)) **The witness has been run** ([4.96](E-method-log.md)), for the first time by this study or the corpus. Killing the worker through CDP between two messages: warm median **46 ms**, first message after the eviction **81 ms**, **added latency 35 ms**, and **5 of 5 runs translated**, so the bar's first clause, *after eviction the first message translates*, holds. The kill is verified as a 1-0-1 transition in the target list; Chrome reuses the target id across a restart, so the id proves nothing. The in-memory state findings above are unchanged: this measures recovery latency, not what the restart forgot. **[new] The startup-race clause is met, and the window was shown to be exercised** ([4.104](E-method-log.md)). The manifest declares `run_at: document_idle`, so the gap between the chat container existing and the script attaching is real. With **no message in the markup**, 19 scheduled across that gap, the extension first marked the page at **41 ms**, **8 messages arrived before that mark**, and **0 were lost**. Everything present at attach is swept by the initial scan and everything after is the observer's, so there is no third case. A first run put only one message inside the window and would have cleared the clause on a sample of one; the attach control is what said so. Scope: this fixture parses faster than a real page, so the gap is the narrowest the product meets. |
| A6 | Injected bytes do not grow release over release without a named reason | open, with a number | [13.4](../thesis/13-results.md): +0.15 percent at the last recorded pass, against a 2 percent margin whose last spend is decomposed per module in `audit_poids.py`. Open rather than closed because the instrument can report green without measuring: on an instrumented build it exits 0 without comparing ([4.49](E-method-log.md)) **The per-row half is measured now** ([4.94](E-method-log.md)): driving the product's own `inject()` in a real browser, DOM capped at 60 as a virtualised list keeps it and layout forced after each row, the warm p50 is **0.2 ms** and the p95 0.3 ms, flat across all four quarters of a 600-row run, against a ceiling of one 60 Hz frame. One main thread saturates near 5000 rows a second. A6's own witness, a deliberate synchronous loop in the row path, moves the p50 to 2.1 ms. The first version of that probe reported 1.3 ms, which was its own accumulating DOM rather than the row. **The last clause is measured now** ([4.107](E-method-log.md)): over 1200 messages the heap's first-half slope is 0.582 KB per message and its second half **0.051**, a ratio of 0.09, flat inside a 20 KB band from message 750 to the end. The same run with the page retaining every message, a leak by construction, holds a ratio of 0.63, so the instrument sees growth when growth is there. At 400 messages the same probe said 0.520 KB per message and could not be read at all: **caps filling and a slow leak have the same slope, and only the decay separates them.** |
| A7 | No interactive control under 24 by 24 | open, with a named reason | The gate measures the targets it finds and asserts no count of them. Executed: with the bar's two controls hidden by their own stylesheet, `bar-widths.mjs` is green at all ten widths and exits 0 ([4.48](E-method-log.md)). The 24 pixel minimum and the declaration that voids it are in one CSS rule. **The queue holds the sizing half and not this one**: it records the gear at 25 by 19 and the pause at 26 by 18 before a fix that made all three 24 tall, and says *removing the rule turns it red at all ten widths*. That is a size regression, which the gate does catch. A control hidden rather than shrunk goes to 0 by 0 and is skipped by the guard, which it does not (4.68) **[new]** Every contrast ratio the sources state beside its own pair was recomputed ([4.98](E-method-log.md)): **26 stated, 5 checkable, 4 exact to two decimals and one wrong**. `inject.css:956` says `#9FA6AD` on `#F4F4F5` is 2.02 where it is 2.24, and the second figure in the same comment, `#5E5E5F` at 5.95, is 5.89. Solving for the ground each implies gives `#e8e8e9`, the chip's hover background, and `#F5F5F5`: **two figures, two grounds, and the comment names a third.** The decision they support holds at the corrected values, so this is a correct conclusion resting on numbers nobody re-derived. The other 21 state a ratio whose ground is a sentence away and are counted, not guessed at. |
| A8 | Zero missing keys, zero identifiers rendered, zero overflow | open, with a number | `scripts/i18n-check.mjs` reports 150 missing keys per locale on files that are complete, so the instrument is wrong rather than the product ([HANDOVER.md](../HANDOVER.md) section 8) |
| A9 | No value outside the declared set; no surface overflowing its frame at any width; the box model does not depend on the host page's reset | open, and **its first clause names a set that does not exist** | Counted from `inject.css`, 1568 lines: **13 declared custom properties**, every one of them `--kt-lp-*` and scoped to the language panel, against **135 distinct literal colours** (96 hex, 39 rgb or rgba), 5 radii, 22 transition and animation declarations and 2 box-shadows, both of them `inset 0 0 0 1px` hairlines rather than decoration. **No document in either repository declares a palette**, so "the declared set" is a third artefact the specification names and nobody made, after the budget file and the ledger ([4.63](E-method-log.md)). The other two clauses hold: `bar-widths.mjs` reports no overflow at any of ten widths, verified by running it in [4.48](E-method-log.md), and the one-declaration box model is among the corpus figures that survived the replication bar |
| A10 | Nothing leaves that the privacy text does not name | open, with a number | [13.7](../thesis/13-results.md) counts the observable surface. The count is of call sites, not of what renders, which is the limit stated wherever such a claim appears. **`PRIVACY.md` compared against the source for the first time, and two things leave that it does not name**: `api.github.com`, contacted every six hours by the self-update check, and **`lingva.ml`, the second host in `LINGVA_POOL`, which receives chat message text**. The policy's table lists four providers and one Lingva host. It was written in `d09b25c` on 2026-05-28, the 2.0.0 commit, and has never been edited since, so it describes a product sixteen releases old ([4.77](E-method-log.md)). **And the claim it does make about stripping is false for the way links are usually typed** ([4.91](E-method-log.md)): both guards require a scheme, outgoing `PROTECT_RE = /(?:https?:\/\/\S+|@[\w.]+)/g` and incoming `URL_RE = /https?:\/\/[^\s<>"']+/gi`, so `kick.com/somechannel`, `twitch.tv/somebody`, `bit.ly/aBcDeF`, `www.example.com/secret` and a bare `example.com/secret` are neither masked nor skipped and reach the configured provider verbatim, **6 of 8 constructed shapes**, measured by [`probe-link-guards.mjs`](D-scripts/probe-link-guards.mjs). Two guards written separately, one blind spot **[new] The DeepL key transits synced storage.** `withoutKey()` is applied at all three `storage.sync.set` sites in `settings.ts` and not at the fourth, `background/index.ts:39`, whose own guard is that the key exists and whose branch fires exactly once: the first time a reader configures DeepL. Observed by recording every value the field takes in sync rather than its end state: empty, **the key**, empty. What removes it is the migration in `loadSettings()`, written to rescue a key left by an older build, so nothing in the code aims at this path ([4.99](E-method-log.md)). Three surfaces of four, which is the guard-fraction rule in running code. |
| A11 | Zero advisories above the floor in the budget file; zero instrumentation in a release bundle proven from the archive; every detection shortcut required-and-written-down or gone, so finding the extension costs a page script a read of rendered content and never a query by name | open, with a number | Advisories: **17 in the full tree, 3 critical and 9 high, and 0 of them in production**. The four runtime dependencies are `franc-min`, `idb-keyval`, `preact` and `zod`, and `npm audit --omit=dev` is zero at every severity, so nothing reaches the bundle. **There is no budget file**, no audit script and no audit config, so the bar's own term is undefined and the floor it refers to does not exist. Instrumentation: `check-strip` proves it from the build rather than the archive, and [4.52](E-method-log.md) closes that gap by other means, since the archive rebuilds byte-identical from the tag. Detection: **at least four shortcuts on the host page**, each one query by name, covering four of the eight categories the axis itself enumerates: `#kt-floating-bar` (`injector.ts:338`), `data-kt-id` written onto Kick's own chat rows (`observer.ts:29`), `data-kt-scheme` set on the document element (`injector.ts:152`), and a class toggled on the document element (`injector.ts:189`), beside 79 distinct `kt-` class names. **No document in either repository counts them**, which is the trap this axis names in its own words ([4.54](E-method-log.md)) **[new] Dead first-party code ships.** `pipeline.ts:206`'s `onWebSocketMessage` is called by nothing in the product and is in `dist/assets/content.js`, **298 minified bytes, 0.128% of 233601** ([4.100](E-method-log.md)). The 2.6.0 changelog says the WebSocket path and its Connection mode setting are gone: the transport is, the setting is still in the schema unread, and the receiving half still ships. It is held in place by five unit tests, four of which use it as a door to live behaviour, so the bytes are load-bearing for the suite rather than merely forgotten. **[new] The detection half is measured now, in the page's own world as the witness requires** ([4.103](E-method-log.md)). Real extension, default settings, one translated message: **6 fixed element ids**, 1 attribute on the document element (`data-kt-scheme`), 0 classes on it, 2 `data-kt` attributes including the per-row `data-kt-id`, 24 prefixed class names, and **0 globals on `window`**. **The bar is not met**: the cheapest detection is one `getElementById` on a fixed id, the moment the extension mounts, which is the *dedicated signal that collapses it to a single call* the axis names as its failure. 13.7's source-derived 11 is a catalogue and this is one state: the document-element class is `kt-hide-original`, toggled only when *keep original* is off, and 24 of the stylesheet's 99 class names are on a page holding one message. The positive is real and the catalogue could not have given it: there is no `kt` global and no object naming the product, which would be the cheapest signal of all. **[new] The archive clauses are read from the archive now** ([4.106](E-method-log.md)). Published zip, sha256 `8c8d7eca262b…`, the digest 4.52 verified, 36 files: **0 instrumentation or source-map markers** across 28 text files and **no `web_accessible_resources`** in the shipped manifest, so both forbidding clauses hold in what a store serves rather than in what a machine built. **No licence or notice file of any kind is in the artifact**, against four runtime dependencies whose licences are not uniform: `franc-min`, `preact` and `zod` MIT, **`idb-keyval` Apache-2.0**. Reported and not judged; the legal question is the developing account's. Presence of those bundles cannot be shown by name, since minification removes package names. And **four icons ship twice**, `icons/*.png` byte-identical to `public/icons/*.png` with the manifest naming only the latter, 3520 bytes; both pairs are in `dist/`, so the archive carries what the build produced. **[new] The third clause has a number now** ([4.108](E-method-log.md)), per name and under both readings of *required*. The product reads **6 of the 8** names back; only **3** are forced to a fixed guessable literal, meaning code outside the file that declares the constant spells the string. **No stylesheet selects any of the six element ids**, so no id's value is load-bearing for appearance, and each is read back only through the constant beside it: a per-install random suffix would satisfy every one of those read-backs. Of the three that are forced, `data-kt-scheme` is structural, 48 CSS rules against a static file that cannot import anything, while `data-kt-id` and `kt-floating-bar` are forced only because `index.ts` repeats strings `observer.ts` and `injector.ts` already hold as constants; importing them puts the floor at one. **Written down: 2 of 8**, both incidental debugging mentions in `.agent/` notes, and no document says the surface is intended. So the clause fails on both terms for six names and on one term for two, and **the remedy differs per name**, which 4.54's blanket could not say. |
| A12 | The end-to-end path passes on every engine claimed in the README and the listings; every engine-specific divergence is a named branch with a comment saying which versions need it; a gate fails when the two builds drift apart in anything else | open, with a number | **The drift property holds, measured.** Both engines were built and their manifests compared key by key: they differ in **exactly 2 of 14**, `background` and `browser_specific_settings`, which are exactly the two branches in `manifest.config.ts`. Nothing else diverges. **No gate protects it**: none of the 40 in the runner matches firefox, gecko, drift or parity, so the property is true and unguarded ([4.64](E-method-log.md)). Comments: **1 of the 2 branches** carries the version reason the bar asks for, and it is a good one, `strict_min_version: '121.0'` with *FF 121+ : ES-module background scripts (`background.type: module`) and storage.session both require it*. The `background` branch has none, although that same sentence is its reason too. Engines: the README claims **four**, Chrome, Brave, Edge and Firefox. Chromium's end-to-end path passes ([4.61](E-method-log.md)); Brave was measured identical to Chromium on one path by the corpus, anonymously, and the signed-in case needs a value 4.47 named; **Edge has never been measured and Gecko has never been run end to end**, because Playwright offers no extension-loading equivalent for Firefox and that is the instrument this account has |
| A13 | Every gate is in a runner or has a written reason it is launched by hand | open, with a number, and **the suite run twice unchanged gives two answers** | **34 of 40, then 38 of 40, back to back with nothing changed** ([4.88](E-method-log.md)). Four gates read fixtures another gate writes and git does not track, `popup.html` from `snapshot.mjs` and `chat-bundle.js` from `chat-live.mjs`, in a runner whose header denies both cases. Two more are red on every run and are not flakes: `lang-panel-measure` reads what the excluded shooter `lang-panel-shoot` alone writes, and `flags-preview` reads a `flags.css` nothing in the repository produces. Budget A13 now carries **N = 2**, since two runs are enough to find it. **The control settles the cause** ([4.89](E-method-log.md)): serially, from the same starting condition, `--jobs 1` gives 38 of 40 every time in 278.4s, and the `GATES` array runs each producer before its reader. The list is in a working order and `--jobs`, defaulting to the core count, discards it, so the default path is the broken one and the serial path is correct. Two runners, 40 offline gates and 9 live ones plus a metrics phase. **19 harness files are launched by nothing** and **7 of them can exit 1** ([12.7](../thesis/12-verification.md), [4.82](E-method-log.md)). This row said *three harnesses that build metrics are outside it*, naming `metrics-offline.mjs`, `latency.mjs` and `run-live.mjs`: `latency.mjs` is launched by `run-live.mjs` as its own phase, `run-live.mjs` is a runner, and the evidence for an axis about runners was taken from [4.49](E-method-log.md), a pass about the weight gate, rather than from [4.33](E-method-log.md), the pass that measured this exact quantity. `metrics-offline.mjs` is the one that holds, and `npm run metrics` reaches it **[new] Five tests are anchored to a feature removed four minor versions ago** ([4.100](E-method-log.md)): they call `onWebSocketMessage`, which the product no longer calls, and four of the five are really about auto-target resolution, target passthrough and the length floor. A13's witness, *delete the feature a gate claims to guard*, does not describe this state and no run of it would reveal it: the subject is already gone and the gates are green because they were never about the subject. |
| A14 | A fresh clone can run the full public verification and get a truthful answer | open, with a number, and **the answer is not truthful** | [13.6b](../thesis/13-results.md). The frame tells every session a fresh clone has no harnesses; it has 56 ([HANDOVER.md](../HANDOVER.md)). Run without a browser driver, which is deliberately not a dependency, the suite reports **32 of 40 gates as `ECHEC` and exits 1**, where 32 exited 2 meaning the prerequisite is absent: `run-gates.mjs` has no branch for exit 2, though `playwright.mjs` states the rule and `run-live.mjs` implements it ([4.86](E-method-log.md)). A clone's honest answer is *8 ran, 32 could not start*; the answer it gives is *32 failed* |
| A15 | One version everywhere, checked by a gate rather than by eye; a rebuild of the tagged commit matches the digest the forge publishes; store state read from the store | open, with a number, and **its reproducibility half closed** | Rebuilt `v2.10.0` in a detached worktree and packed both archives: sha256 `8c8d7eca262b…` and `4f8450494d7e…`, **2 of 2 byte-identical to the digests GitHub publishes**, on Node 22 where CI builds on the 20 that `.nvmrc` pins ([4.52](E-method-log.md)). Version agreement is not closed: six places, **three answers**, 2.10.0 in `package.json`, the built manifest, the tag and the release assets, 2.9.2 on the Chrome Web Store, 2.7.0 on AMO, both read from the stores. The store lag is a pending submission blocked on kil rather than a disagreement. **A gate checks it now, and it is in the wrong repository**: `version-agreement.mjs` reads the package, the derived manifest, the built manifest, the newest tag, the tag reachable from HEAD and, with `--with-network`, the release and its assets, and reports **6 of 6 agreeing on 2.10.0**, with four witnesses each red on a row only it can see ([4.83](E-method-log.md)). The axis still does not close: the bar says *checked by a gate*, this study's own rule says a gate outside the runner of the thing it guards protects nothing, and none of the clone's 40 offline gates matches version, release, manifest or tag. `state.mjs` is a generated report, not a gate. What closes A15 is moving this file into the clone's runner, which is the developing account's to do |
| A16 | No screenshot older than the feature it shows, and none showing a real person's handle or message; localised documents current or carrying a version line | open, with a documented trade and a number | Screenshots: 4 of 5 are produced by the `captures-readme` gate from a fabricated room and were last committed on the tag date. The fifth carries four real handles from a live channel and is **deliberately kept**, with the reason written in `screenshots/README.md`, which also states that the image it replaced carried them. So the clause is not broken by oversight; it rests on a trade the owner made ([4.53](E-method-log.md)). Localised READMEs: 3 of 3 head their what-new section at 2.8.1 while the product is 2.10.0, 3 of 3 link that heading to `releases/latest` so the heading and its destination disagree, and 0 of 3 carry the version line the bar allows instead. The sections pinned to `/tag/v2.8.0` in the same files are correct, so the only unpinned link in each is the one that goes stale. **The queue already held this**, as an open item at `PLAN.md:486` with the same 2.8.1 against 2.10.0, and it names a second defect this study missed: the alt strings written for the new images were written without a native reader, in es, pt-BR and ja (4.65). **Changelog against releases, compared for the first time**: the two records disagree about which versions exist. `2.8.0` is tagged and published with its own release notes and has **no changelog section**; `2.8.1` has the largest section in the file, 253 lines, and **no tag and no release**; their contents are different work, so it is not a numbering slip. `2.3.1` likewise has a section and no tag, and `2.9.3` is tagged without a section, which the queue explains. The queue also says *2.9.3 and 2.9.4 were tagged*: **there is no v2.9.4 tag**. And the three localised READMEs head their what-new at 2.8.1, a version with neither tag nor release, under a link to `releases/latest` that serves 2.10.0 ([4.69](E-method-log.md)). **2.8.1's work did ship**, verified against the source: `constants.ts` carries the section's reasoning verbatim, `coalescer.ts` carries the adaptive window and the counting correction it describes, and this study's own runtime reading of `coalesce.window` at p50 40 ms is `MIN_BATCH_WINDOW_MS` exactly. So what is missing is a version label, not a feature ([4.71](E-method-log.md)) |
| A17 | Every remote branch is live or has a named reason | open, with a number | Branches counted, and the unmerged two never opened ([4.40](E-method-log.md)). Clones re-swept: the queue records finding *a second clone*, and there are **three** of this repository on the machine, two at the same HEAD `226a176` and one stale at `6c5d42b`, 2026-06-15, holding five uncommitted entries. The queue says nothing unique remains in it and that is now **verified by hash**: `transliterationGuard.ts` and its test are byte-identical to the published versions, `cc221f0de97714b1` and `5c0d1ec812f945be`, so the commit named *Rescue transliterationGuard exactly as it was found* did exactly that. The three modified source files were not compared, master having moved far past June ([4.67](E-method-log.md)) |
| A18 | Zero required decisions before the first translation appears; every empty and failure state shows an actionable sentence; older settings load without loss; time to first visible translation under the ceiling in the budget file | open, with a number, and **its first clause closed** | **Zero decisions, confirmed by running it**: `translate-offline.mjs` on a `mkdtemp` profile, no settings and no reader action, ends with the translation rendered under the message. The whole chain is exercised, extension load through content script, observer, pipeline, service worker and engine adapter to the injected node, and it passes. Timing: `metrics-offline.mjs` was run for the first time in this study and gives, offline with the engine answered locally, **`e2e.cloud` p50 44 ms over 26 samples, p95 45, max 82**, of which `leg.coalesce.wait` is p50 41 and `coalesce.window` is 40. So roughly 40 of the 44 ms is the coalescing window the product chooses to wait and about 3 ms is everything else, and chapter 13's cloud p50 of 1618 ms is therefore almost entirely the provider ([4.61](E-method-log.md)). This is per message, not cold-start to first paint, so the bar's own quantity is still *not set* in [appendix G](G-budget.md). **The listing half has a number that moved**: the queue's open item on the install rate records Chrome at 2.9.2, updated 2026-08-30, **75 users**, and the store served **112** on two readings in this session against the same version and the same date. Up 49 percent on a listing that did not change, which bears on that item's own experiment, since it proposes a rewrite and the baseline moved without one (4.66) **Time to first visible translation is measured now** ([4.95](E-method-log.md)): on a profile that has never run the extension, p50 **278 ms** from navigation to the translated text in the DOM, 5 of 5 runs. The control, a second message on the same page, is p50 **47 ms**, so **231 ms is the part paid once**: extension load, content script, observer attach, MV3 worker boot. That control replicates `metrics-offline.mjs`'s counter-derived `e2e.cloud` p50 of 44 ms by a different technique, and 40 of those milliseconds are the deliberate `MIN_BATCH_WINDOW_MS`. The engine is answered locally, so a provider round trip is on top of both. **[new] The empty-and-failure-state clause is met in 15 of 16 sentences** ([4.105](E-method-log.md)). The chat catalogue holds 48 keys across nine locales and `msg.coverage.test.ts` matches call sites to entries in both directions, refuses an empty scan and fails on a stale exemption: 43 assertions, run here and passing. **`pipeline.ts:169` returns a template literal instead of calling `localised()`**, so the minimum-length reason never enters the catalogue and is English in every locale. The coverage test cannot see it, because a string that never calls `msg()` is not a call site; the report is complete and correct about everything it can reach. It is also the sentence most worth localising: of the sixteen skip reasons, eight name something the reader controls and this is the only one that prints their own configured value. |
| A19 | Every counter has its numerator and denominator written beside it; no denominator containing a population it does not describe; no decision recorded against an unreachable counter; a numerator whose denominator does not exist under the same prefix is a gate failure | open, with a number | 20 counters and 16 timings enumerated from the call sites. Applying the pairing rule the bar names, **3 of 20 counters have no denominator readable beside them**: `dom.attach`, `drop.recycled.unrescued` and `google.batch.fallback`, and two of the three carry a word in their own name that implies a ratio. The other 17 pair as siblings, as extensions, or as dynamic families that sum to their own total. The code uses two pairing shapes and the bar names one, which is why a first mechanical count said 12 and was wrong ([4.55](E-method-log.md)). Separately: [13.4](../thesis/13-results.md) publishes a 6.7 percent in-tab cache hit rate and 1.0 percent persistent, 8 hits in 821 lookups, **without saying they come from an instrumented build**, in a chapter that states elsewhere that the release strips instrumentation. No gate checks the pairing convention, which the axis says in its own words a gate can check |
| A20 | Nothing from a chat message or a provider response reaches a markup sink | open, with a number | `probe-render-sinks.mjs` counts sinks and text-node writes and refuses to classify which carry untrusted text, deliberately ([appendix D](D-scripts/)). The unclassified remainder is why this is not closed |
| A21 | A full storage area degrades and never corrupts: the reader keeps their settings whatever happens to the cache; requests under refusal never exceed the count in the budget file and a refusal escalates rather than repeats; no write lost under concurrent tabs; nothing resetting on a day boundary resets twice | open, with a number, and **two clauses closed by construction** | Storage: **three separate areas**, settings in `chrome.storage.sync`, stats and metrics in `storage.local`, and the translation cache in IndexedDB through `idb-keyval`, used in `cache.ts` and nowhere else. Filling the cache cannot evict a settings key, so the reader keeps their settings whatever happens to the cache, by architecture rather than by assertion. Escalation: per-provider health with `consecutiveFailures` and `cooldownUntilMs`, and the candidate list is filtered to providers whose cooldown has expired, so a refusal moves to the next engine instead of repeating. Rate: **derived from the ladder rather than measured**, sustained `rate_limit` settles at one attempt per 10 s per provider, **6 a minute per provider and 24 a minute across the four**, and [appendix G](G-budget.md) now carries that with its derivation. Open: `saveSettings` merges, `{ ...current, ...patch }`, but the read and the write are not atomic, so two tabs that both read before either writes can still lose the later field. The ladder itself is self-declared guessed, in a comment saying its three numbers *have never been read back*; `metrics-offline.mjs` read them for the first time in [4.61](E-method-log.md) and they were empty, because the fixture engine never refuses ([4.62](E-method-log.md)) **[new] The day-boundary clause is met, over a population of one** ([4.102](E-method-log.md)). Enumerated across every tracked source file, the only day key in the product is `stats.ts:7`, `new Date().toISOString().slice(0, 10)`; `metrics.ts` rolls nothing daily, MyMemory's daily cap is the provider's own and is read off a 200 body, and the DeepL budget is monthly and counted by DeepL. That one surface carries **8 tests**, run here and passing, covering append order, a zero-traffic day dropped rather than stored as 0 percent, the 7-day cap matching `HISTORY_DAYS`, and idempotence on a day already archived. The *in any zone* half needs no measurement: `toISOString()` is UTC by definition, so the boundary cannot move with the reader's zone, and a probe confirming that would be measuring the language. A reader at UTC+13 does see the counter reset in the early afternoon, which the clause does not forbid and which is recorded as a separate observation. |
| A22 | Every finding carries a second, differently-shaped confirmation; zero counts over text where a structured form exists; zero exit codes read through a pipe; zero quotations that are translations; zero verifier sharing its technique with its subject; the pass reports its own false-positive count beside its findings | open, with a number, and **its clauses name this session's own failures** | Nine measurements went through the replication bar and **five changed** ([14.2b](../thesis/14-limits.md)). Clause by clause against this session: *exit codes read through a pipe* happened once and was caught in the same command, an `$?` after a pipe reading `tail`'s status rather than the gate's; *quotations that are translations* found **3 of 78** reshaped, and three blind spots that hid two more ([4.72](E-method-log.md), [4.74](E-method-log.md)); *counts over text where a structured form exists* is how the laughter table was first miscounted by a regular expression against a file whose keys are French, where importing the module was the structured form and is what [chapter 6](../thesis/06-laughter.md) had used. The false-positive count the last clause asks for: **about nine findings drafted and withdrawn before publication across 34 passes**, listed in 4.44, 4.53, 4.54, 4.55, 4.58, 4.63, 4.71, 4.73 and the broken matcher in [4.78](E-method-log.md) |

## The instrument each axis needs, and whether it exists here

Step 2 of the specification's own first pass: *walk the index and mark each axis
with the instrument it needs and whether that instrument exists on this machine
today. Now the queue is real.* This table is that step, taken late. "Exists"
means it was run, or it was run in this session and is known to work, not that
something with a plausible name is present.

| Axis | Instrument it needs | Here today |
|---|---|---|
| A1 | An instrumented build plus real traffic, counters read back | build yes, traffic no |
| A2 | The benches in `src/content/langDetect*.test.ts` | yes, run |
| A3 | `compose-kick-live.mjs`, `compose-live.mjs` | present, not run here |
| A4 | Source reading plus a live Kick DOM | source yes, live DOM no |
| A5 | Playwright with the worker evicted by hand | possible, never run |
| A6 | `audit_poids.py` on a release build | yes, run |
| A7 | `bar-widths.mjs` against a copied build | yes, run |
| A8 | `scripts/i18n-check.mjs` | present and known wrong |
| A9 | A live browser against the host's art direction | no |
| A10 | `probe-render-sinks.mjs` | present, classification deliberately manual |
| A11 | `npm audit` for the tree; [`probe-detection.mjs`](D-scripts/probe-detection.mjs) in the page's world; [`probe-archive.mjs`](D-scripts/probe-archive.mjs) on the published zip | yes, all run (4.103, 4.106) |
| A12 | A signed-in session in a second browser | **no**, and the missing value is named (4.47) |
| A13 | `run-gates.mjs` | yes, read |
| A14 | A fresh clone and the public verification | yes |
| A15 | A worktree at the tag, a rebuild, the forge's digests | yes, run (4.52) |
| A16 | `probe-orphan-assets.mjs` and reading | yes, run (4.53) |
| A17 | `git` | yes |
| A18 | Playwright on a cold profile | yes, run (4.61) |
| A19 | `metrics-offline.mjs` | present, not run here |
| A20 | `probe-render-sinks.mjs` | present |
| A21 | A browser with its storage quota filled | possible, never run |
| A22 | This method log and the replication bar | yes |

Seven axes need something that exists and has never been pointed at them. One,
A12, needs something this account does not have, and 4.47 named it down to the
registry value. **No axis is blocked by a missing instrument that could not be
built**, which is a different and more uncomfortable answer than "the work is
hard".

## The other artefact the specification asked for, and nobody made

Seven of the 22 bars, **A3, A5, A6, A11, A13, A18 and A21**, state their
threshold as a ceiling, a floor, a count or a rate *in the budget file*. There
is no budget file. The specification says so itself, under the heading about a
first pass: *the ledger and the budget file are asked for by this document and
will not be there the first time it is read.* Both were asked for, neither was
made, and seven bars are unreadable as a consequence rather than as an
accident. This file closes the first of the two. The second is a page of
numbers and remains the cheapest unbuilt thing in this study (4.57).

## What this table says about the stop condition

**All 22 axes carry a verdict, and the first stop condition is met.** It is the
first of the specification's three to be satisfied, and it was unreadable until
this file existed. The number that matters more sits beside it: **none of the 22
is closed.** Every one is *open with a number*, which the specification accepts
as a resting state and which is the honest description of an audit that measured
a great deal and finished nothing. A15 came closest, its reproducibility half
closed against a published digest.

The other two conditions are not met: `PLAN.md` holds 7 open items beside its 6
blocked on kil, and every pass in this session produced a new measurement. All
three are required, so meeting one changes what is countable rather than whether
the work is done. The point of the row is that until someone writes the
number down beside the Bar, nobody can tell an unmeasured axis from a measured
one, and the stop condition cannot be read at all.

## The honest limit of this table

The mapping from a chapter's subject to an axis was made by a reader, not by a
script, and a script could not make it: chapter 13 never names an axis, which is
the gap this file exists to close. So the evidence column is a judgement about
where a number belongs, and a different reader could place some of them
differently. What is mechanical is only the axis list itself, which
`axis-ledger.mjs` keeps in step with the specification.
